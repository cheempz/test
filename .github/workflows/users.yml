name: Container Test 

on: 
  workflow_dispatch:

jobs:

  vm-test:
    runs-on: ubuntu-latest 
    
    steps:
      - name: show user
        run: |
          id
          env | sort

  container-test:
    runs-on: ubuntu-latest 
    container:
      image: node:16-alpine3.11      

    steps:
      - name: Checkout ${{ github.ref }}
        uses: actions/checkout@v2
        with:
          path: foo

      - name: Show Info
        run: |
          id
          ls -alR
          npm install @appoptics/apm-bindings
      
